version: '3.8'

services:
  playwright-app:
    container_name: playwright-dev-container
    image: playwright-dev-image
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    volumes:
      - ../:/playwright-app # hot reload, no need restart docker if only .py changed
    ports:
      - '9801:9801'